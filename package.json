{
  "name": "gatsby-source-yext",
  "description": "Gatsby source plugin for Yext",
  "version": "0.2.0",
  "license": "MIT",
  "main": "./dist/gatsby-node.js",
  "files": [
    "./dist/*",
    "gatsby-node.js"
  ],
  "scripts": {
    "clean": "del-cli dist",
    "build": "tsc",
    "develop": "tsc --watch",
    "test": "jest",
    "prepare": "npm run clean && npm run build"
  },
  "keywords": [
    "gatsby",
    "gatsby-plugin"
  ],
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/lodash": "^4.17.25",
    "del-cli": "^7.0.0",
    "gatsby": "5.17.0-react19.0",
    "minimatch": "^10.0.3",
    "typescript": "^7.0.2"
  },
  "peerDependencies": {
    "gatsby": "^5.0.0"
  }
}
